---
ansible-navigator:
  execution-environment:
    container-engine: podman
    enabled: True
    image: f5-ee:latest
    pull:
      arguments:
        - "--tls-verify=false"
      policy: never
  logging:
    level: critical
  playbook-artifact:
    enabled: false