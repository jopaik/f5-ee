---
ansible-navigator:

  execution-environment:
    container-engine: podman
    enabled: true
    environment-variables:
      pass:
      - ONE
      - TWO
      - THREE
      set:
        KEY1: VALUE1
        KEY2: VALUE2
        KEY3: VALUE3
    image: f5-ee:latest
    pull:
      policy: never

  logging:
    level: critical

  playbook-artifact:
    enabled: false

  mode: stdout