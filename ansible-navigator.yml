---
ansible-navigator:
  execution-environment:
    container-engine: podman
    enabled: true
    environment-variables:
      pass:
      - ONE
      - TWO
      - THREE
      set:
        KEY1: VALUE1
        KEY2: VALUE2
        KEY3: VALUE3
    image: localhost/f5-ee
    pull:
      policy: never
  logging:
    level: critical